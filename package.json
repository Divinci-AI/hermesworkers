{
  "name": "hermesworkers",
  "version": "0.1.0",
  "description": "Run the Hermes personal AI assistant inside a Cloudflare Sandbox container.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.12.4",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8",
    "wrangler": "^4.112.0"
  }
}
